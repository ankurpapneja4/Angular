// Declaration 
let person : {
                name: string;
                age : number;
             };

// Instantiate
person = {
            name : 'Rose',
            age  : 20
         };

// Output
console.log( person.name );
console.log( person.age  );